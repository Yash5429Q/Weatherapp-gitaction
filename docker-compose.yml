version: "3.9"

services:
  db:
    image: postgres:17
    container_name: weather-db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: weatherdb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  backend:
    image: yash5429q/weather-backend:latest   # ✅ use image from Docker Hub
    container_name: weather-backend
    ports:
      - "4000:4000"
    env_file:
      - ./weather-backend/.env
    depends_on:
      - db

  frontend:
    image: yash5429q/weather-frontend:latest   # ✅ use image from Docker Hub
    container_name: weather-frontend
    ports:
      - "5173:80"
    env_file:
      - ./weather-frontend/.env
    depends_on:
      - backend

volumes:
  pgdata:
